# Environment template for the API server
# Rename to `.env` (keep the leading dot) when running locally
# or copy/paste the keys into the Vercel project settings.

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-db-password

REDIS_URL=redis://localhost:6379

JWT_SECRET=change-me-in-production

PORT=4000 